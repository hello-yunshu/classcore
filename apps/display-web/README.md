# Display Client — product surface

大屏只读渲染端。只消费 `ProjectedStageState<audience=display>` 和 public-pseudonymous Presentation Widget projection。禁止请求 Identity Directory、真实 participantId/姓名映射或课堂控制 Command。
