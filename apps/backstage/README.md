# Backstage / Operations — product surface

运维与控制面，不是课堂 Participant Role。D7 最小职责：创建 Session、名单/Identity 映射、凭证/二维码、Feature Policy、Preflight、Diagnostics、日志/导出、Lesson/Presentation 选择。LAN HTTP 第一版默认仅 Server Host `localhost` 开放。
