import { getSurfaceDescriptor, CLIENT_RUNTIME_BUDGETS } from '@classroom/surfaces';
export const surface = getSurfaceDescriptor('simulation-rehearsal');
export const runtimeBudget = CLIENT_RUNTIME_BUDGETS['simulation-rehearsal'];

